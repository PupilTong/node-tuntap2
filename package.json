{
  "name": "tuntap2",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "types": "index.ts",
  "scripts": {
    "install": "prebuild-install --runtime napi --tag-prefix '' || node-gyp rebuild || tsc",
    "build": "node-gyp rebuild && tsc",
    "build:dev": "node-gyp rebuild --debug",
    "test": "sudo mocha tests/*",
    "coverage": "sudo nyc mocha tests/*"
  },
  "author": "pupiltong",
  "license": "Apache-2.0",
  "dependencies": {
    "bindings": "^1.5.0",
    "jsonpath": "^1.1.1",
    "node-addon-api": "^4.0.0",
    "prebuild-install": "^7.0.0"
  },
  "binary": {
    "napi_versions": [
      3
    ]
  },
  "devDependencies": {
    "@types/jsonpath": "^0.2.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.8",
    "mocha": "^9.1.4",
    "nyc": "^15.1.0",
    "should": "^13.2.3",
    "typescript": "^4.5.4"
  }
}
